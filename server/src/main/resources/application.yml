spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/17farm?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 6720


  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG
          type:
            trace
cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_ACCESS_KEY}
    s3:
      bucket: jihoon-bucket
    region:
      static: ap-northeast-2
    stack:
      auto: false
jwt:
  secret-key: MakeASecretKeyINeedALongLongSentencesButItsTooHardIWannaDoLoginSuccessWhyThisIsShortIWANNALOGIN
#  access-token-expiration-minutes: 30
#  refresh-token-expiration-minutes: 420
server:
  servlet:
    encoding:
      force-response: true